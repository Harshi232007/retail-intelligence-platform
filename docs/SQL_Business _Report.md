# SQL Business Report
## Retail Intelligence Platform
**Week 2 - Day 3**

---

## Project Objective

The objective of this analysis is to use SQL queries to analyze retail sales data and generate business insights that help improve decision-making.

---

## Dataset

- Dataset Name: Superstore Dataset
- Database: retail.db
- Table: sales

---

# SQL Queries and Results

## 1. Total Sales

**SQL Query**

```sql
SELECT SUM(Sales) AS Total_Sales
FROM sales;
```

**Result**

Total Sales = 12642905 

**Observation**

This represents the total revenue generated from all orders.

---

## 2. Total Profit

**SQL Query**

```sql
SELECT SUM(Profit) AS Total_Profit
FROM sales;
```

**Result**

Total Profit = 1467457.29128

**Observation**

This represents the total profit earned by the business.

---

## 3. Average Sales

**SQL Query**

```sql
SELECT AVG(Sales) AS Average_Sales
FROM sales;
```

**Result**

Average Sales = 246.498440241763

**Observation**

This shows the average sales amount per order.

---

## 4. Highest Sale

**SQL Query**

```sql
SELECT MAX(Sales) AS Highest_Sale
FROM sales;
```

**Result**

Highest Sale = 22638

**Observation**

This is the highest-value order in the dataset.

---

## 5. Lowest Profit

**SQL Query**

```sql
SELECT MIN(Profit) AS Lowest_Profit
FROM sales;
```

**Result**

Lowest Profit = -6599.978

**Observation**

This is the largest loss recorded in the dataset.

---

## 6. Sales by Category

**SQL Query**

```sql
SELECT Category,
       SUM(Sales) AS Total_Sales
FROM sales
GROUP BY Category;
```

**Result**

| Category | Total Sales |
|----------|------------:|
| Furniture | <Result> |
| Office Supplies | <Result> |
| Technology | <Result> |

**Observation**

The category with the highest sales is **<Office Supplies
>**.

---

## 7. Profit by Category

**SQL Query**

```sql
SELECT Category,
       SUM(Profit) AS Total_Profit
FROM sales
GROUP BY Category;
```

**Result**

| Category | Total Profit |
|----------|-------------:|
| Furniture | <Result> |
| Office Supplies | <Result> |
| Technology | <Result> |

**Observation**

The most profitable category is **<Category Name>**.

---

## 8. Sales by Region

**SQL Query**

```sql
SELECT Region,
       SUM(Sales) AS Total_Sales
FROM sales
GROUP BY Region;
```

**Result**

| Region | Total Sales |
|---------|------------:|
| Central | <Result> |
| East | <Result> |
| South | <Result> |
| West | <Result> |

**Observation**

The region with the highest sales is **<Region Name>**.

---

## 9. Top 10 Customers

**SQL Query**

```sql
SELECT Customer_Name,
       SUM(Sales) AS Total_Sales
FROM sales
GROUP BY Customer_Name
ORDER BY Total_Sales DESC
LIMIT 10;
```

**Result**

Paste the top 10 customers returned by your query.

**Observation**

These customers contribute significantly to the company's revenue.

---

## 10. Top 10 Products

**SQL Query**

```sql
SELECT Product_Name,
       SUM(Sales) AS Total_Sales
FROM sales
GROUP BY Product_Name
ORDER BY Total_Sales DESC
LIMIT 10;
```

**Result**

Paste the top 10 products returned by your query.

**Observation**

These are the highest-selling products in the retail business.

---

## 11. Categories with Sales Greater Than 100000

**SQL Query**

```sql
SELECT Category,
       SUM(Sales) AS Total_Sales
FROM sales
GROUP BY Category
HAVING SUM(Sales) > 100000;
```

**Result**

Paste the query result.

**Observation**

These categories generated sales greater than 100000.

---

## 12. Total Orders by Region

**SQL Query**

```sql
SELECT Region,
       COUNT(*) AS Total_Orders
FROM sales
GROUP BY Region;
```

**Result**

| Region | Total Orders |
|---------|-------------:|
| Central | <Result> |
| East | <Result> |
| South | <Result> |
| West | <Result> |

**Observation**

The region with the highest number of orders is **<Region Name>**.

---

# Overall Business Insights

1. Total sales generated by the business are **<Result>**.
2. Total profit earned is **<Result>**.
3. The highest-selling category is **<Category>**.
4. The most profitable category is **<Category>**.
5. The region generating the highest revenue is **<Region>**.
6. The customer contributing the highest sales is **<Customer Name>**.
7. The highest-selling product is **<Product Name>**.
8. The business should focus on profitable categories while improving the performance of lower-performing categories.

---

# Conclusion

This SQL analysis demonstrates how business data can be queried to generate valuable insights for decision-making. The queries provide information about sales, profit, customer behavior, product performance, and regional performance, which are essential for building the Retail Intelligence Platform.